import type { SpellMetadata } from '../../lib/types'

export const metadata: SpellMetadata = {
  category: 'Text',
  aspectRatio: '1:1',
  size: 'default',
  dependencies: ['motion'],
}

export default metadata
